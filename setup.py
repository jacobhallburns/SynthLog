# This is the standard Python Package for SynthLog
from setuptools import setup
setup(
   name='SynthLog',
   version='1.0',
   install_requires=[
      'customtkinter',
      'CTkMenuBar',
      # Add Other Dependencies here
   ],
)